import template from '../../../html/controls/text.html';

export default /** @ngInject */  function($rootScope) {
    return {
        template: template,
        replace: true,
        controller: /** @ngInject */ function($scope, $rootScope) {

        }
    }
}
